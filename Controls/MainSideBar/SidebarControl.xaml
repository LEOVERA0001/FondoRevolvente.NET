<UserControl x:Class="Fondo_Revolvente.Controls.MainSideBar.SidebarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="250">
    
    <Border Background="#2C3E50" BorderThickness="0,0,1,0" BorderBrush="#34495E">
        <StackPanel Margin="0,20,0,0">
            <TextBlock Text="Fondo Revolvente" 
                      Foreground="White" 
                      FontSize="18" 
                      FontWeight="Bold" 
                      HorizontalAlignment="Center" 
                      Margin="0,0,0,30"/>
            
            <Button Name="DashboardButton" 
                    Content="📊 Dashboard" 
                    Background="Transparent"
                    Foreground="White"
                    BorderThickness="0"
                    Padding="20,15"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Left"
                    FontSize="14"
                    Cursor="Hand"
                    Click="DashboardButton_Click"/>
            
            <Button Name="MinistracionesButton" 
                    Content="💰 Ministraciones" 
                    Background="Transparent"
                    Foreground="White"
                    BorderThickness="0"
                    Padding="20,15"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Left"
                    FontSize="14"
                    Cursor="Hand"
                    Click="MinistracionesButton_Click"/>
        </StackPanel>
    </Border>
</UserControl>